#include <stdio.h>
/**
 * main - the entry point
 * Return: is 0
 */
int main(void)
{
	char v;

	int w;

	v = 'a';
	w = 0;
	while
		(w < 10)
		{
			putchar(w + '0');
			w++;
		}
	while
		(v <= 'f')
		{
			putchar(v);
			v++;
		}
	putchar('\n');
	return (0);

}
